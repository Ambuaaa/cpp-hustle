#include <bits/stdc++.h>
using namespace std;

int main()
{
  long long a, b;
  cin >> a >> b;

  if (a == b || (a <= 1 && b <= 1))
  {
    cout << "Yes";
  }
  else
  {
    cout << "No";
  }

  // bro how can be two factorial be same ???????/
}